package org.example.core.basic.identity;

import org.example.core.basic.ClassLevelSite;

public class ClassTypeNode extends ClassLevelSite {
    protected ClassTypeNode(String className) {
        super(className, className);
    }
}
